# Frontend configuration.
# Copy to ".env" only if your backend runs somewhere other than the default.
#
# Base URL of the backend API. Defaults to http://localhost:8000 if unset.
VITE_API_BASE_URL=http://localhost:8000
